import React from 'react';

const Info = ({location}) => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        flexDirection: 'column',
      }}
    >
      Dit is de info pagina
    </div>
  );
};

export default Info;
